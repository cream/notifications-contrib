from distutils.core import setup

setup(
    name = 'Notifications Contrib Libraries',
    version = '0.1',
    package_dir = {'cream.contrib.notifications': 'notifications'},
    packages = ['cream.contrib.notifications'],
    )
